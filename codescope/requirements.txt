# Core
openai>=1.0.0
flask>=3.0.0
flask-cors>=4.0.0

# Evaluation metrics
rouge-score>=0.1.2
sacrebleu>=2.3.1

# Fine-tuning (run on GPU / Google Colab)
# pip install peft transformers datasets accelerate bitsandbytes